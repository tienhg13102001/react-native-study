# Sao chép file này thành .env.local rồi điền giá trị thật (KHÔNG commit .env.local).
# Bắt buộc tiền tố EXPO_PUBLIC_ thì Expo mới nhúng vào app.
# ⚠️ Giá trị EXPO_PUBLIC_ nằm plain-text trong bundle app — KHÔNG thật sự bí mật.

EXPO_PUBLIC_API_URL=https://script.google.com/macros/s/XXXX/exec
EXPO_PUBLIC_API_TOKEN=doi-thanh-chuoi-bi-mat-cua-ban-123xyz
